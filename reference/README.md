# reference/

Read-only vendored external material, kept only for pattern lookup on a hard
problem. Nothing here is installed, activated, synced, or loaded by default, and
nothing here is authority — cite an idea, never a rule.

- `ecc/` — pinned sparse checkout of `affaan-m/ECC` @ `005eff40` (own upstream
  remote; do not commit local edits into it). Curated component list and rationale
  in `ecc.lock.json`.

To retire an entry, move it to `.agents/_retired/` and record the reason in
`instructions/CURRENT_STATE.md`.
