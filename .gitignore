# Independent repositories retained outside this Zandi-owned agent-assets repository.
instructions/

# Vendored read-only reference checkouts (own upstream remotes).
reference/ecc/

# Retired frameworks — inert, keep their own Git history, never tracked here.
_retired/
