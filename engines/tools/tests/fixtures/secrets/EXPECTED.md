# fixture EXPECTED schema (NOT canonical — test data)

Declared expected secret files (no values):

- `valid.env` — placeholder OK
- `empty.env` — empty file
- `malformed_syntax.env` — bad syntax line
- `control_byte.env` — embedded control/ANSI bytes
- `missing.env` — declared but absent
