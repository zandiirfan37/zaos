# Zandi library

`library/` holds deeper reference knowledge: repository cards, standards, and source material. It is not automatically loaded.

`skills/` contains executable reusable guidance for agent behavior. A skill may point here only when a live task needs the supporting detail.
